import 'package:flutter/material.dart';

class AccessId extends StatelessWidget {
  const AccessId({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}